# Hugo build output (GitHub builds this for us; no need to commit it)
/public/
/resources/
.hugo_build.lock

# OS noise
.DS_Store
Thumbs.db

# Claude Code local state (the create-pr skill in .claude/skills is tracked)
.claude/worktrees/
.claude/settings.local.json

# Vendored third-party skill (installed by git clone, not part of the site)
.claude/skills/book-to-skill/

# Generated study skill (derived from a third-party paper, stays local)
.claude/skills/self-evolving-coding-agents/
